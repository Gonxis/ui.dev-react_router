### (Practice) Sidebars - The Solution


App.js
```js
import * as React from "react";
import { BrowserRouter as Router, Route, Routes, Link } from "react-router-dom";

const routes = [
  {
    path: "/",
    main: () => <h1>Home</h1>,
    emojis: () => <div>ð ð ð ð ð </div>
  },
  {
    path: "/rainbows",
    main: () => <h1>Rainbows</h1>,
    emojis: () => <div>ððððð</div>
  },
  {
    path: "/bears",
    main: () => <h1>Bears</h1>,
    emojis: () => <div>ð»ð»ð»ð»ð»</div>
  }
];

export default function App() {
  return (
    <Router>
      <div className="wrapper">
        <div className="sidebar">
          <ul className="nav">
            <li>
              <Link to="/">Home</Link>
            </li>
            <li>
              <Link to="/rainbows">Rainbows</Link>
            </li>
            <li>
              <Link to="/bears">Bears</Link>
            </li>
          </ul>
          <Routes>
            {routes.map(({ path, emojis }) => (
              <Route key={path} path={path} element={emojis()} />
            ))}
          </Routes>
        </div>

        <Routes>
          {routes.map(({ path, main }) => (
            <Route key={path} path={path} element={main()} />
          ))}
        </Routes>
      </div>
    </Router>
  );
}
```


styles.css
```css
.wrapper {
  display: flex;
}

.nav {
  display: flex;
  flex-direction: column;
  padding: 0;
}

.nav li {
  list-style-type: none;
}

.sidebar {
  width: 200px;
  padding-right: 20px;
  margin-right: 50px;
  border-right: 1px solid var(--white);
  color: #babcc1;
}

.body {
  margin: 0 50px;
}
```


---------------------------------------------------------------------------------

Comments:

- What about using children? Is it possible?

- The icons are not displayed properly. Seeing: ð»ð»ð»ð»ð»

- Is there anything wrong with setting main and emoji properties in routes to be components instead of functions? I know it works I'm just curious if one solution is better than the other?

Solution in video:

```js
const routes = [
    {
        path: '/',
        main: () => <h1>Home</h1>,
        emoji: () => <div>🏠🏠🏠</div>
    }
...
```

Alternate Solution?:

```js
const routes = [
    {
        path: '/',
        main: <h1>Home</h1>,
        emoji: <div>🏠🏠🏠</div>
    }
...
```

  - That's a good solution as well. One benefit of functions is you could pass arguments to them, but in our example that doesn't matter much. -Tyler

  - Thanks for clarifying that Tyler!
