### (Practice) Programmatically Navigate - Declarative - The Solution


App.js
```js
import * as React from "react";
import { BrowserRouter as Router, Link, Routes, Route } from "react-router-dom";
import Loading from "./Loading";

const Home = React.lazy(() => import("./Home"));
const Newsletters = React.lazy(() => import("./Newsletters"));
const Dashboard = React.lazy(() => import("./Dashboard"));

export default function App() {
  return (
    <Router>
      <ul>
        <li>
          <Link to="/">Home</Link>
        </li>
        <li>
          <Link to="/newsletters">Newsletters</Link>
        </li>
        <li>
          <Link to="/dashboard">Dashboard</Link>
        </li>
      </ul>

      <hr />

      <React.Suspense fallback={<Loading />}>
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/newsletters" element={<Newsletters />} />
          <Route path="/dashboard" element={<Dashboard />} />
        </Routes>
      </React.Suspense>
    </Router>
  );
}
```


index.js
```js
import React from "react";
import ReactDOM from "react-dom/client";
import "./theme.css";
import "./styles.css";
import App from "./App";

const rootElement = document.getElementById("root");
const root = ReactDOM.createRoot(rootElement);

root.render(
  <React.StrictMode>
    <div className="yellow-border" />
    <div className="container">
      <App />
    </div>
  </React.StrictMode>
);
```


---------------------------------------------------------------------------------

Comments:

- Without the Suspense element, I get an error message : A component suspended while responding to synchronous input. This will cause the UI to be replaced with a loading indicator. To fix, updates that suspend should be wrapped with startTransition.

As far as I understood it, the suspense is good to use, but not required, is that wrong?

  - It's required if you want to use React.lazy.

  - Tbh, that wasn't clear from the textual description. Maybe it could be useful to add that it's required? Now it says: "To tell React what to show, you can use React's Suspense component passing it a fallback element."
